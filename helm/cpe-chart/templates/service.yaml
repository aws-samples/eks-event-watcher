apiVersion: v1
kind: Service
metadata:
  name: {{ include "cpe-chart.fullname" . }}
  labels:
    {{- include "cpe-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cpe-chart.selectorLabels" . | nindent 4 }}
